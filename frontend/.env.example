# Vite only exposes env vars to client code if they're prefixed with VITE_.
# Relative path so the SPA calls back to the same origin it was served from,
# letting Nginx/Coolify proxy /api/ to the backend. Works in any environment.
VITE_API_URL=/api
